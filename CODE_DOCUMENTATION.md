# 飞书 AI 研究助手 - 代码功能详解

## 项目概述

这是一个基于飞书的 AI 助手系统，支持股票持仓管理、个股分析、市场行情查询、GitHub 趋势搜索、arXiv 论文搜索等功能。

### 核心架构

```
用户发送消息 → main_v2.py → 意图识别 (Kimi) → Skill 执行 → 返回结果
```

**主要文件位置：**
- `ai-research-assistant/feishu-assistant/main_v2.py` - 主程序入口
- `ai-research-assistant/feishu-assistant/skills/` - 技能模块目录
- `ai-research-assistant/feishu-assistant/core/` - 核心组件目录

---

## 指令系统详解

### 一、快捷命令 (以 `/` 开头)

系统支持快捷命令和多种简写形式，通过模糊匹配实现。

| 完整命令 | 简写 | 功能说明 | 示例 |
|---------|------|---------|------|
| `/help` | `/h`, `/he` | 显示帮助菜单 | `/help` |
| `/market <市场>` | `/m`, `/ma` | 查询市场行情 | `/market US` 或 `/m cn` |
| `/github <关键词>` | `/g`, `/gh` | 搜索 GitHub 趋势 | `/github ai-agent` |
| `/paper <主题>` | `/p`, `/pa` | 搜索 arXiv 论文 | `/paper LLM` |
| `/chat <消息>` | `/c`, `/ch` | 智能对话 | `/chat 你好` |
| `/clear` | `/cl` | 清除会话历史 | `/clear` |
| `/status` | `/s`, `/st` | 查看系统状态 | `/status` |
| `/portfolio` | `/po`, `/pt` | 查询持仓 | `/portfolio` |
| `/持仓` | - | 查询持仓（中文） | `/持仓` |
| `/reset` | `/r`, `/rs` | 重置持仓（需确认） | `/reset` |
| `/reset confirm` | - | 确认重置持仓 | `/reset confirm` |
| `/track` | `/tr`, `/tk` | 追踪持仓盈亏 | `/track` |
| `/追踪` | - | 追踪持仓盈亏（中文） | `/追踪` |

**命令匹配逻辑** (main_v2.py:195-228):
1. 首先尝试精确匹配
2. 然后尝试模糊匹配（支持前几个字母简写）
3. 如果有多个匹配，提示用户选择

---

### 二、自然语言指令 (无需 `/` 前缀)

#### 1. 持仓查询类

| 自然语言 | 功能 | 代码位置 |
|---------|------|---------|
| `我的持仓` | 查询当前持仓 | main_v2.py:248 |
| `持仓情况` | 查询当前持仓 | main_v2.py:248 |
| `查看持仓` | 查询当前持仓 | main_v2.py:248 |

#### 2. 交易记录类 (智能解析)

系统支持从自然语言中自动解析交易信息：

**支持的格式：**
```
买入 [股票] [数量]股 价格[价格]
卖出 [股票] [数量]股 [价格]元
买入 [股票] [数量]股 [价格]美元
买入 [股票] [数量]股 [价格]港币
记录买入 [股票] [数量]股 [价格]元
```

**示例：**
| 用户输入 | 解析结果 |
|---------|---------|
| `买入茅台 100股 价格1500` | 买入 茅台 100股 ¥1500 |
| `卖出腾讯 50股 400元` | 卖出 腾讯 50股 ¥400 |
| `买入AAPL 10股 180美元` | 买入 AAPL 10股 $180 |
| `买入腾讯 100股 400港币` | 买入 腾讯 100股 HK$400 |
| `记录买入宁德时代 200股 220元` | 买入 宁德时代 200股 ¥220 |

**解析逻辑** (portfolio_skill.py:578-669):
1. 首先尝试正则表达式解析 (`parse_trade_message`)
2. 如果正则失败但包含交易关键词，使用 Kimi 大模型解析 (`parse_with_llm`)
3. 智能识别币种（人民币/美元/港币）

#### 3. 智能对话

发送任意非命令文本，会通过大模型 (Kimi) 智能识别意图并执行相应功能。

---

## 技能模块详解

### 1. 持仓管理技能 (PortfolioSkill)

**文件：** `skills/portfolio_skill.py`

**功能：**
- 记录股票买卖交易
- 查询当前持仓
- 重置/清空持仓
- 支持多币种（人民币/美元/港币）

**数据库表结构** (portfolio_skill.py:92-117):
```sql
CREATE TABLE transactions (
    id INTEGER PRIMARY KEY,
    user_id TEXT NOT NULL,
    stock_name TEXT NOT NULL,
    stock_code TEXT NOT NULL,
    market TEXT NOT NULL,
    action TEXT NOT NULL,  -- 'buy' 或 'sell'
    price REAL NOT NULL,
    currency TEXT DEFAULT 'CNY',
    shares INTEGER NOT NULL,
    total_amount REAL NOT NULL,
    trade_date TEXT NOT NULL,
    created_at TEXT DEFAULT CURRENT_TIMESTAMP
)
```

**持仓查询逻辑** (portfolio_skill.py:343-405):
```python
# 按股票和币种分组汇总
SELECT
    stock_name, stock_code, market, currency,
    SUM(CASE WHEN action = 'buy' THEN shares ELSE -shares END) as total_shares,
    SUM(CASE WHEN action = 'buy' THEN total_amount ELSE -total_amount END) as total_cost
FROM transactions
GROUP BY stock_code, currency
HAVING total_shares > 0
```

**输出格式** (portfolio_skill.py:464-541):
- 按币种分组显示（人民币账户/港币账户/美元账户）
- 每只股票显示：持仓数、成本均价、当前价格、盈亏金额和比例、仓位占比
- 底部显示总持仓统计

**存储路径：** `/opt/feishu-assistant/data/portfolio.db`

---

### 2. 个股分析技能 (StockSkill)

**文件：** `skills/stock_skill.py`

**功能：**
- 查询个股实时行情
- 获取分析师评级和目标价
- 获取最新研报
- 使用 Kimi 生成 AI 投资分析

**支持的股票：**

**A股** (部分示例):
| 名称 | 代码 | 名称 | 代码 |
|-----|------|-----|------|
| 茅台/贵州茅台 | sh600519 | 宁德时代 | sz300750 |
| 比亚迪 | sz002594 | 招商银行 | sh600036 |
| 中国平安 | sh601318 | 中信证券 | sh600030 |

**港股**:
| 名称 | 代码 | 名称 | 代码 |
|-----|------|-----|------|
| 腾讯/腾讯控股 | hk00700 | 阿里巴巴 | hk09988 |
| 美团 | hk03690 | 小米 | hk01810 |
| 百度 | hk09888 | 网易 | hk09999 |

**美股**:
| 名称 | 代码 | 名称 | 代码 |
|-----|------|-----|------|
| 苹果/AAPL | usAAPL | 微软/MSFT | usMSFT |
| 谷歌/GOOGL | usGOOGL | 亚马逊/AMZN | usAMZN |
| 特斯拉/TSLA | usTSLA | 英伟达/NVDA | usNVDA |

**ETF基金** (部分示例):
| 名称 | 代码 | 名称 | 代码 |
|-----|------|-----|------|
| 上证50ETF | sh510050 | 沪深300ETF | sh510300 |
| 创业板ETF | sh159915 | 科创板50ETF | sh588000 |
| 芯片ETF | sh512760 | 人工智能ETF | sh159819 |

**代码解析逻辑** (stock_skill.py:357-408):
1. 优先匹配名称映射表
2. 6位数字 → A股（6开头上海，其他深圳）
3. 5位数字 → ETF基金
4. 美股代码格式 → 自动添加 us 前缀

**数据来源：**
| 数据类型 | 来源 |
|---------|------|
| A股/港股实时行情 | 腾讯财经 API (`qt.gtimg.cn`) |
| 美股实时行情 | Yahoo Finance API |
| 分析师评级 | 东方财富研报 API |
| AI 分析 | Kimi 大模型 |

**输出示例：**
```
📈 茅台 (600519) A股
━━━━━━━━━━━━━━━━━━━━
💰 当前价格: 1850.00 (+2.35%)

📊 今日行情:
• 今开: 1805.00
• 最高: 1860.00
• 最低: 1795.00
• 昨收: 1808.00

📈 交易数据:
• 成交量: 32.50万手
• 换手率: 0.85%
• 市盈率: 35.20
• 流通市值: 2.32万亿

👨‍💼 分析师观点:
• 综合评级: 买入
• 目标价: 2200.00 (+18.9%)
• 覆盖机构: 28家

🤖 AI 投资分析:
[Kimi 生成的分析内容]
```

---

### 3. 持仓追踪技能 (PortfolioTrackerSkill)

**文件：** `skills/portfolio_tracker_skill.py`

**功能：**
- 定时追踪持仓盈亏
- 生成持仓报告
- 发送通知

**存储路径：** `/opt/feishu-assistant/data/portfolio_tracker.db`

---

### 4. 其他技能

| 技能 | 功能 | 文件 |
|-----|------|------|
| MarketSkill | 市场行情查询 | skills/market_skill.py |
| GitHubSkill | GitHub 趋势搜索 | skills/github_skill.py |
| PaperSkill | arXiv 论文搜索 | skills/paper_skill.py |
| ChatSkill | 智能对话 | skills/chat_skill.py |

---

## 消息处理流程

### main_v2.py 消息处理流程

```
1. 接收飞书消息
      ↓
2. 去重检查 (processed_msgs)
      ↓
3. 获取/创建用户会话
      ↓
4. 判断消息类型:
   ├── 以 `/` 开头 → _handle_command()
   └── 其他 → _handle_natural_language()
      ↓
5. 执行 Skill
      ↓
6. 发送回复
```

### _handle_command() 处理逻辑 (main_v2.py:120-242)

```python
# 1. 解析命令和参数
parts = text.split(maxsplit=1)
cmd = parts[0].lower()
args = parts[1] if len(parts) > 1 else ""

# 2. 精确匹配
if cmd in command_definitions:
    skill = registry.get(cmd_def["skill"])
    return await skill.execute(**cmd_def["params"])

# 3. 模糊匹配（简写支持）
cmd_input = cmd[1:]  # 去掉 /
for full_cmd, definition in command_definitions.items():
    full_name = full_cmd[1:]
    if full_name.startswith(cmd_input):
        matches.append((full_cmd, definition))
    elif cmd_input in definition.get("shortcuts", []):
        matches.append((full_cmd, definition))
```

### _handle_natural_language() 处理逻辑 (main_v2.py:244-293)

```python
# 1. 检查是否是持仓查询
if any(kw in text for kw in ["持仓", "我的股票", "持仓情况", "查看持仓"]):
    return await portfolio_skill.execute(action="query", user_id=...)

# 2. 检查是否是交易记录
trade_info = await portfolio_skill.smart_parse_trade(text)
if trade_info:
    return await portfolio_skill.execute(action="record", ...)

# 3. 使用大模型识别意图
plan = await intent_recognizer.recognize(
    user_input=text,
    skills_schema=registry.get_all_schemas(),
    context=session
)
skill = registry.get(plan["skill"])
return await skill.execute(**plan["parameters"])
```

---

## 环境配置

### 必需的环境变量

| 变量名 | 说明 | 示例 |
|-------|------|------|
| FEISHU_APP_ID | 飞书应用 ID | cli_xxxxx |
| FEISHU_APP_SECRET | 飞书应用密钥 | xxxxx |
| KIMI_API_KEY | Kimi 大模型 API Key | sk-xxxxx |
| GITHUB_TOKEN | GitHub Token (可选) | ghp_xxxxx |

### 可选的环境变量

| 变量名 | 说明 | 默认值 |
|-------|------|------|
| PORTFOLIO_DB_PATH | 持仓数据库路径 | /opt/feishu-assistant/data/portfolio.db |

---

## 部署方式

### 1. 本地部署 (Mac/Linux)

```bash
# 安装依赖
pip install lark-oapi httpx

# 启动服务
python ai-research-assistant/feishu-assistant/main_v2.py
```

### 2. 服务器部署

使用部署脚本自动部署：
```bash
./deploy/main_v2.py deploy --host <服务器IP>
```

---

## 定时任务

### 持仓追踪定时任务

**文件：** `scripts/portfolio_tracker_cron.py`

**功能：** 每天收盘后自动追踪持仓盈亏

**设置方式：**
```bash
# 使用 crontab
crontab -e
# 添加: 0 16 * * 1-5 /usr/bin/python3 /path/to/portfolio_tracker_cron.py
```

---

## 代码结构

```
ai-research-assistant/
├── feishu-assistant/
│   ├── main_v2.py              # 主程序入口
│   ├── core/
│   │   ├── intent_recognizer.py # 意图识别
│   │   └── skill_registry.py    # 技能注册表
│   ├── skills/
│   │   ├── base_skill.py       # 技能基类
│   │   ├── stock_skill.py      # 个股分析
│   │   ├── portfolio_skill.py  # 持仓管理
│   │   ├── portfolio_tracker_skill.py # 持仓追踪
│   │   ├── market_skill.py     # 市场行情
│   │   ├── github_skill.py     # GitHub搜索
│   │   ├── paper_skill.py      # 论文搜索
│   │   └── chat_skill.py       # 智能对话
│   └── README.md
├── scripts/
│   └── portfolio_tracker_cron.py  # 定时任务
└── deploy/
    └── main_v2.py              # 部署脚本
```

---

## 常见问题

### 1. 命令简写不生效

确保使用正确的简写格式，例如 `/po` 而非 `po`。

### 2. 持仓查询显示"暂无持仓"

需要先记录交易，例如发送 `买入茅台 100股 价格1500`。

### 3. 股价获取失败

检查网络连接，部分数据源需要代理。

### 4. AI 分析不可用

确保已配置 KIMI_API_KEY 环境变量。

---

*文档更新时间: 2026-02-25*
